apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-khoai.fullname" . }}-test-connection"
  labels:
    {{- include "helm-khoai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-khoai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
